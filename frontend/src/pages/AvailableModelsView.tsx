import { EmptyState } from '@/components/console/AdminState';

export function AvailableModelsView() {
  return <EmptyState title="Available Models" body="Catalog inventory coming in a later task." />;
}
